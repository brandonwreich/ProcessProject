package sample.controller;

public class SampleRunner {
	
	public static void main (String [] args) //Always the same and always written here
	{
		//Needed to start for every project. The only thing that will change is the name (SampleController)
		SampleController myApp = new SampleController();
		myApp.start();
		
}
}
